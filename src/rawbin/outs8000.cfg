MEMORY {
    ZP:   start = $0000, size = $0100, type = rw, define = yes;
    RAM:  start = $8000, size = $0100, type = rw;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    CODE:     load = RAM, type = rw, start = $8000;
}