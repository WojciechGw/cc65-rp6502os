MEMORY {
    ZP:       start = $0000, size = $0100, type = rw, define = yes;
    CPUSTACK: file = "", start = $0100, size = $0100;
    RAM:      file = %O, start = $0200, size = $FC00,define = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    CODE:     load = RAM, type = ro;
    RODATA:   load = RAM, type = ro;
}
